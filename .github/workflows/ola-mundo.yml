name: Ola, Mundo!

on: [push]

jobs:
  ola-mundo-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run script
        run: |
          echo "Ola, Mundo!"